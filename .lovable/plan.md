
The user wants me to run a read-only SQL query on `feed_products` to diagnose why most Thailand slots fall to Step 3 fallback. I'm in plan mode (read-only) so I'll present a plan to run the query and share results.

Simple plan: approve → I run the SELECT via `supabase--read_query` and share the table verbatim with brief interpretation per slot.
