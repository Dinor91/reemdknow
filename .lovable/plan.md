

## Update Telegram Bot Token

The current `TELEGRAM_BOT_TOKEN` secret needs to be replaced with the full token you just provided.

### Steps
1. Update the `TELEGRAM_BOT_TOKEN` secret with the new value
2. Test the bot by triggering `/start` via the edge function to verify it works

### No code changes needed
The edge function code remains the same — only the secret value changes.

